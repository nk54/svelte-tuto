---
import PrimaryLayout from "@/layouts/PrimaryLayout.astro";
import { Five } from "@/components/five";
---

<PrimaryLayout>
  <div class="flex flex-col gap-4 m-12">
    <h1 class="font-bold text-3xl">Pattern 5 : Composable/Hook</h1>
    <p class="text-muted-foreground text-lg">
      Pattern inspiré de Vue/React : une fonction réutilisable retourne un objet avec état et méthodes. 
      Encapsule la logique métier dans un composable qui peut être appelé depuis n'importe quel composant. 
      Favorise la réutilisation du code et la séparation des préoccupations.
    </p>
    <div class="bg-blue-50 dark:bg-blue-900/20 p-4 rounded-lg">
      <p class="mb-2 font-semibold text-sm">🔄 Équivalents dans d'autres frameworks :</p>
      <ul class="space-y-1 text-sm">
        <li><strong>React :</strong> Custom Hooks (useState + useEffect)</li>
        <li><strong>Vue 3 :</strong> Composables (Composition API)</li>
        <li><strong>Angular :</strong> Standalone functions avec inject()</li>
        <li><strong>POO :</strong> Pattern Strategy/Composition over Inheritance</li>
        <li><strong>Autre :</strong> Extension Methods (.NET), Extension Functions (Kotlin), Traits (Rust)</li>
      </ul>
    </div>
    <div class="bg-purple-50 dark:bg-purple-900/20 p-4 border border-purple-200 dark:border-purple-800 rounded-lg">
      <p class="mb-2 font-semibold text-purple-900 dark:text-purple-100 text-sm">✅ Utilisez ce pattern si :</p>
      <ul class="space-y-1 text-purple-800 dark:text-purple-200 text-sm">
        <li>✓ Vous voulez <strong>plusieurs instances indépendantes</strong></li>
        <li>✓ Vous privilégiez l'<strong>explicite</strong> (props) à l'<strong>implicite</strong> (global)</li>
        <li>✓ Vous voulez une logique <strong>testable en isolation</strong></li>
        <li>✓ Vous construisez une <strong>bibliothèque de composants réutilisables</strong></li>
        <li>✓ Vous avez besoin de <strong>plusieurs listes filtrées</strong> sur la même page</li>
      </ul>
    </div>
    <Five client:load />
  </div>
</PrimaryLayout>
