---
import PrimaryLayout from "@/layouts/PrimaryLayout.astro";
import { One } from "@/components/one";
---

<PrimaryLayout>
  <div class="flex flex-col gap-4 m-12">
    <h1 class="font-bold text-3xl">Pattern 1 : Props & Callbacks</h1>
    <p class="text-muted-foreground text-lg">
      L'approche la plus simple et directe. Le composant parent gère tout l'état avec <code>$state</code> 
      et <code>$derived</code>, puis passe les données et callbacks aux composants enfants via props. 
      Idéal pour les petites applications ou lorsque la hiérarchie des composants est peu profonde.
    </p>
    <One client:load />
  </div>
</PrimaryLayout>
