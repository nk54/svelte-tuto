---
import PrimaryLayout from "@/layouts/PrimaryLayout.astro";
import { Three } from "@/components/three";
---

<PrimaryLayout>
  <div class="flex flex-col gap-4 m-12">
    <h1 class="font-bold text-3xl">Pattern 3 : Store avec Runes</h1>
    <p class="text-muted-foreground text-lg">
      Modernise le pattern store avec les runes Svelte 5 (<code>$state</code>, <code>$derived</code>). 
      Une factory function retourne un objet avec des getters réactifs. 
      Combine la puissance des stores avec la syntaxe moderne des runes pour une réactivité fine-grained.
    </p>
    <Three client:load />
  </div>
</PrimaryLayout>
