---
import PrimaryLayout from "@/layouts/PrimaryLayout.astro";
import { Four } from "@/components/four";
---

<PrimaryLayout>
  <div class="flex flex-col gap-4 m-12">
    <h1 class="font-bold text-3xl">Pattern 4 : Context API</h1>
    <p class="text-muted-foreground text-lg">
      Utilise <code>setContext()</code> et <code>getContext()</code> pour partager l'état dans l'arbre des composants. 
      Évite le prop drilling en permettant aux composants descendants d'accéder directement au contexte. 
      Excellent pour les fonctionnalités qui nécessitent un état partagé dans une sous-arborescence spécifique.
    </p>
    <Four client:load />
  </div>
</PrimaryLayout>
